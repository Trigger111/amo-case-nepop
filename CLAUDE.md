# AMO Case — Продуктовый аналитик (Genesis Analytics Camp 3.0)

Первый продуктовый кейс. Приложение по подписке (персональные тренировки), продаётся
через квиз-воронку на сайте, рынки US/EU/Tier-1, бесплатной версии нет. ~6 месяцев назад
цену подняли 6.99 → 9.99/нед на основе A/B-теста, который показал, что конверсия не упала.
Сейчас CEO сомневается: цена +42%, а в Revenue такого роста не видно. Разбираемся.

## КАК СО МНОЙ РАБОТАТЬ (важнее всего)

Пользователь (Александр) делает кейс, чтобы **научиться** — он пишет **весь код анализа сам**.
- Новичок в Python, SQL (проседает на джоинах), A/B-статистике.
- Моя роль: **критический наставник**. Объясняю концепты, ревьюю его код, ловлю ошибки в
  рассуждениях, оспариваю необоснованные предположения.
- **НЕ писать за него код анализа и не выдавать готовые посчитанные ответы.** Даю по одному
  строительному блоку / одному новому механику + цель — он собирает сам.
- Маленькие шаги. Не грузить плотными однострочниками (переваривает тяжело) — раскладывать.
- Проверять данные молча в своём терминале можно, но не раскрывать числа, которые он должен
  вывести сам.
- Язык общения: русский.

## Окружение

- Ноутбук `amo_case_analysis.ipynb`, открыт в VS Code (ядро Python 3.14).
- Установлено: pandas, numpy, matplotlib, seaborn, scipy, statsmodels.
- Данные: `data/*.csv` (6 файлов), закоммичены в git → синхронизируются между компами.
- **На новой машине:** `git pull` → открыть ноутбук → выбрать ядро Python 3.14 → Run All,
  чтобы пересоздать переменные (состояние ядра между компами НЕ переносится, надо перезапустить).
- Переменные в ноутбуке: `df_test_data`, `df_order_data`, `df_otp_data`, `df_refunds_data`,
  `df_user_data`, `df_marketing_spend_data`, и очищенный `df_test_clean`.

## Данные (6 таблиц)

- **test_data** (события A/B-воронки, янв–фев): `user_id`, `event_date`, `event_name`
  (click / purchase / **otp_purchase** — апсел; в презентации он назван sub_purchase),
  `group` (control=6.99 / test=9.99), `country_group` (EU/USA/Tier1), `platform`,
  `payment_method`, `product_name`, `channel`. 1 строка = 1 событие.
- **order_data** (ребилы, раскатка на 50%, янв–мар): `user_uuid`, `date_started`,
  `Product Name`, `order_date`, `Trial Period Days`(7), `Rebill Period Days`(7). Много строк/юзер.
- **otp_data**: `user_uuid`, `Product Name`, `revenue`(=15). Покупатели OTP-апсела.
- **refunds_cb_data**: `user_uuid`, `Product Name`, `refund`(0/1), `chargeback`(0/1), `price`.
  Чарджбек = возврат стоимости + штраф $30.
- **user_data**: `user_uuid`, `region`, `platform`, `payment_type`, `age_segment`. 1 строка/юзер.
- **marketing_spend_data**: `date`, `channel`, `country_group`, `group`, `clicks`, `cpc`, `spend`.

Грабли: ключ джоина — `user_id` в test_data, но `user_uuid` в остальных. Регистр product_name
разный ("Weekly 6.99" vs "weekly 6.99").

## Задания

1. **A/B:** правильно ли решили по конверсии в покупку? Как цена повлияла на конверсию в
   основной продукт И в апсел для разных сегментов?
2. **LTV на 1.5 мес** для 6.99 vs 9.99 по сегментам, с учётом рефандов, чарджбеков, OTP-апселов.
3. Реально ли ARPU вырос на +42% при цене +42%? Сколько заработали/потеряли за **26.01–16.03**
   из-за перехода на дорогую цену? Как изменился доход с 1 юзера?
4. **ROAS** по сегментам (канал, регион, ценовая группа). Меняет ли это рекомендацию по цене?
   Изменилась ли окупаемость при новой цене vs старой?
Финал: рекомендация по цене.

## Прогресс

### Блок 0 — качество данных: ГОТОВО
- 1131 полный дубль строк, все в control/click → почищено в `df_test_clean` (`drop_duplicates()`).
- **Контаминация Tier1 (ключевая находка):** в тестовой группе ВСЕ Tier1-юзеры по цене 6.99,
  а не 9.99 (видно через `crosstab(country_group, product_name)`). То есть Tier1 никогда не
  тестировался валидно → это случайный A/A. Валидное ценовое сравнение = только EU+USA.
  Ещё это значит, что раскатка 9.99 была неравномерной → часть головоломки с Revenue.
- test_data = окно A/B-теста; order/user/refunds/otp = поздняя раскатка на 50% → разные
  популяции и периоды, не смешивать.

### Задание 1 — конверсия: В ПРОЦЕССЕ
Конверсия click→purchase (уник. покупатели / уник. кликеры) по region×group,
посчитана пользователем самостоятельно (вложенный цикл):
| регион | control 6.99 | test 9.99 |
|---|---|---|
| EU | 5.24% | 5.41% |
| USA | 7.03% | 7.11% |
| Tier1 (обе 6.99) | 6.42% | 6.89% |

Наблюдение: конверсия 9.99 ≥ 6.99 везде, но дельты крошечные. Разброс в Tier1-A/A (0.47 п.п.)
БОЛЬШЕ, чем «эффект» в EU+USA (0.13 п.п.) → эффект, вероятно, в пределах шума. Нужен стат-тест.

z-тесты сделаны (сам, циклом for по регионам): EU z=-0.885 p=0.376; USA z=-0.344 p=0.731;
Tier1 (A/A) z=-1.019 p=0.308. Значимости нет нигде; |z| в A/A больше, чем в реальных
сравнениях → эффект цены на CR основного продукта = шум. Утверждение слайда «конверсия
не изменилась» подтверждено на валидных EU+USA. Контаминация: 10% юзеров тест-арма (Tier1)
реально видели 6.99. Пользователь также разобрал: для-циклы, nunique vs shape, p-value.

### Подсказки авторов кейса (получены 15.07.2026, записаны из черновика Александра)
- **Смотреть динамику по дням, не тоталы**: как «бежал» тест во времени в разных разрезах;
  LTV тоже раскладывать во времени, не в тотале. Это наш главный пробел на сейчас.
- **«Какую дату взять в A/B — модели тут ошибаются»**; после конца теста есть «кучки/хвосты»,
  «юзеры из группы могут прилетать когда угодно» → валидное окно теста надо определить по
  данным, а не брать диапазон файла. НЕ спойлерить — пусть найдёт через графики динамики.
- LTV: «оцениваем по тестовой таблице, считаем по order_date» (со слов ментора, «но не точно»).
- Триал = платное начало подписки, первая неделя (версия ментора); вторая оплата юзера =
  первый ребилл. Трактовку строк order_data (все платежи vs только ребилы) решить с
  Александром при Блоке 2 — это ключевая развилка логики LTV.
- Рефанд = возвращает компания; чарджбек = возвращает банк, + $30 штраф компании.
- Все таблицы склеить в одну систему координат (ключ user_uuid); есть юзер(ы) без
  age_segment — незначащие точечные истории можно удалять.
- Процессное: «копать в одну точку несколько дней — может быть ошибка» (не залипать).

### Задание 1 — ЗАКРЫТО, числа на окне 10–24.01.2025
- **Валидное окно уточнено до 10–24.01** (было 09–24): 9 января — «одноногий» день,
  работал только control (4073 кликера, 275 покупок), test-арма НЕ было — баг сплитовалки.
  Нашли через unstack-график кликеров по группам во времени. Исключён; влияние на выводы
  крошечное. Плюс низкообъёмные хвосты (05–08, 25.01–23.02) и все 85 otp-«сирот».
- z-тесты CR в окне: EU p=0.29, USA p=0.54, Tier1 (A/A) p=0.23 → CR не изменилась.
- **Апсел (attach = otp/purchase): EU 24.59%→16.36%, z=5.6, p<0.0001 — ЗНАЧИМОЕ падение
  (−8.2 п.п., −33% относит.). USA 19.56%→17.40% (p=0.12, не значимо, тот же наклон вниз).
  Tier1 A/A: p=0.91 — чисто.** Главная находка: компания апсел не проверяла.
- Блок 1.1 (доход с покупателя = цена + attach×15, цена из product_name, не из группы!):
  control→test: EU 10.68→12.44 (+16.5%), USA 9.92→12.60 (+27.0%), Tier1 11.57→11.52 (~0 ✓).
  Разложение разрыва к наивным +42.9%: EU = механика 14.8 п.п. + каннибализация 11.6 п.п.;
  USA = 12.6 + 3.3. Каннибализация — то, что отличает EU от USA.
- **ГРАБЛИ (ловить впредь):** `period_start` был задан в ДВУХ ячейках → смена в одной
  оставила другую на 09 → ноутбук считал наполовину на 09, наполовину на 10. Правило:
  константа определяется один раз (single source of truth). Александр это чинит.
- Типичные ошибки Александра в формулировках (ловить в ревью): путает п.п. и %;
  сравнивает тоталы разноразмерных армов («добавили $8328» — нельзя, армы 1596 vs 1534
  покупателя; нормировать на покупателя); говорит «Tier1 неправильно разбит» вместо
  «разбивка исправна, не доставлена цена»; складывает проценты разных баз (17+27=44);
  забывает p-value в выводах. Механики освоены: for, dict, append, DataFrame(rows),
  pivot, тернарник, set-операции, assert, groupby+plot, proportions_ztest.

### Задание 1 — ЗАКРЫТО
Выводы оформлены в `conclusions_task1.md` (в корне репо; пп. 3–4 дописаны мной по
просьбе уставшего Александра, пп. 1, 2, 5 — его). Механика окончательно объяснена как
«потолок»: рост целого = средневзвешенный рост частей; механика_пп = наивный% × доля
апсела в выручке control. Опциональный хвост: сегменты platform/channel/payment_method
не разбирали (age_segment для теста недоступен — только в user_data раскатки, overlap=0).

### Дальше
1. **Задание 2 (LTV) — стартовало. Авторы дали официальную рамку (17.07.2026):**
   - Вопрос: реально ли доход С ЮЗЕРА вырос на +42% при цене +42%?
   - **`order_data`: каждая строка = ФАКТИЧЕСКОЕ СПИСАНИЕ (ребилл).** Это снимает развилку
     «что такое строка» — LTV строим, суммируя списания (кол-во строк юзера × цена).
   - Данные: diff_days (order_date−date_started) = 7,14,21…49, шаг 7, diff=0 НЕТ;
     у ВСЕХ 215088 юзеров есть строка diff=7, дальше по убыванию (75269→29891→…) —
     это retention-кривая. date_started с 25.01 (сразу после теста).
   - Остаточная под-развилка (пусть Александр решит и обоснует): триал 0–7 дней —
     платный или бесплатный? В order_data списаний в day-0 нет → по букве авторов
     «каждая строка = списание» триал бесплатный, LTV = строки × цена. Версия ментора
     (устно, «не точно») — триал платный. На относительное сравнение 6.99 vs 9.99
     влияет слабо (триал одинаков в обеих когортах).
   - **Две гипотезы эрозии (сердце задания):** (1) retention — мобайл жалуется, что
     9.99-юзеры меньше в аппе → меньше ребилов; (2) рефанды+чарджбеки растут (саппорт),
     чарджбек = возврат цены + штраф $30/ордер. Проверить, съедают ли они +42%.
   - Формула LTV/юзер (1.5 мес): строки×цена + OTP($15 если в otp_data) − рефанды
     − чарджбеки(цена+$30). Горизонт 1.5 мес → цензурирование (не все дожили 45 дней).
   - Сегменты — join с user_data по user_uuid (первый джоин Александра, проседает —
     разжевать merge). Цена когорты — из Product Name (регистр «weekly», не «Weekly»!).
2. Затем Задание 3 (Revenue/ARPU, окно 26.01–16.03, по order_date) → Задание 4 (ROAS)
   → финальная рекомендация по цене.
